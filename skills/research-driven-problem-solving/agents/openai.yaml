interface:
  display_name: "ARPS"
  short_description: "Human-like reasoning for complex non-physical problems."
  default_prompt: "Use $arps to clarify the real problem, model stakeholders and causes, compare options, checkpoint progress, and choose the next bounded action."

policy:
  allow_implicit_invocation: true
